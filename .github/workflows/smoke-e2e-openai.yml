name: Smoke test — OpenAI end-to-end ✅

on:
  workflow_dispatch: {}
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  actions: read

jobs:
  smoke-e2e:
    name: E2E smoke test (start server + call /api/recycle/recommend)
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm ci

      - name: Start server in background
        run: |
          nohup npm start > server.log 2>&1 &
          echo $! > server.pid

      - name: Start Admin Runner in background
        run: |
          nohup node admin-runner.js --server > admin-runner.log 2>&1 &
          echo $! > admin-runner.pid
          sleep 1

      - name: Wait for server health
        run: |
          for i in {1..30}; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/api/health || true)
            echo "Attempt $i: health=$STATUS"
            if [ "$STATUS" = "200" ]; then
              curl -s http://localhost:3001/api/health | jq -r '.status, .uptime'
              break
            fi
            sleep 2
          done

      - name: Perform recycle recommend request
        run: |
          set -o pipefail
          echo '{"items":["plastic bottle"], "location":"test"}' > /tmp/payload.json
          HTTP_CODE=$(curl -s -w "%{http_code}" -o /tmp/rec.json -X POST -H "Content-Type: application/json" -d @/tmp/payload.json http://localhost:3001/api/recycle/recommend)
          echo "HTTP_CODE=$HTTP_CODE"
          cat /tmp/rec.json
          if [ "$HTTP_CODE" != "200" ]; then
            echo "recommend endpoint failed: $HTTP_CODE" >&2
            exit 1
          fi
          OK=$(jq -r '.ok' /tmp/rec.json)
          if [ "$OK" != "true" ]; then
            echo "recommend returned ok!=true" >&2
            exit 1
          fi

      - name: Verify Admin Runner endpoints through Proxy
        run: |
          set -o pipefail
          echo "Verifying permissions API..."
          HTTP_CODE_PERMS=$(curl -s -w "%{http_code}" -o /tmp/perms.json http://localhost:3001/api/matrix/api/admin/permissions)
          echo "Permissions HTTP CODE: $HTTP_CODE_PERMS"
          cat /tmp/perms.json
          if [ "$HTTP_CODE_PERMS" != "200" ]; then
            echo "Admin Permissions endpoint failed!" >&2
            exit 1
          fi

          echo "Verifying app layout template release..."
          HTTP_CODE_LAYOUT=$(curl -s -w "%{http_code}" -o /tmp/layout.html http://localhost:3001/api/matrix/api/admin/layout)
          echo "Layout HTTP CODE: $HTTP_CODE_LAYOUT"
          if [ "$HTTP_CODE_LAYOUT" != "200" ]; then
            echo "Admin Layout template endpoint failed!" >&2
            exit 1
          fi
          grep -q "TEMPLATE RELEASE v1.0.1" /tmp/layout.html || { echo "Template release watermark not found!"; exit 1; }

      - name: Cleanup server
        if: always()
        run: |
          if [ -f admin-runner.pid ]; then kill $(cat admin-runner.pid) || true; fi
          if [ -f server.pid ]; then kill $(cat server.pid) || true; fi
          pkill -f "node server.js" || true
          pkill -f "node admin-runner.js" || true
