name: Sync Branches

on:
  push:
    branches:
      - main
      - bigtree

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      
      - name: Setup Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
      
      - name: Sync main → bigtree
        if: github.ref == 'refs/heads/main'
        run: |
          git fetch origin
          if git show-ref --quiet --verify refs/remotes/origin/bigtree; then
            git checkout bigtree
            git merge origin/main --allow-unrelated-histories -m "Auto-sync: main → bigtree" || true
          else
            git checkout -b bigtree
            git push -u origin bigtree
          fi
          git push origin bigtree || true
      
      - name: Sync bigtree → main
        if: github.ref == 'refs/heads/bigtree'
        run: |
          git fetch origin
          git checkout main
          git merge origin/bigtree --allow-unrelated-histories -m "Auto-sync: bigtree → main" || true
          git push origin main || true
